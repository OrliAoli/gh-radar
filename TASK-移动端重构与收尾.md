移动端重构与收尾任务.md
内容由AI生成，仅供参考
反馈
去元宝做同款
移动端页面重构 + 收尾任务 · 给 CodeBuddy
授权：连续做完，中途不用问我。只在文末「停下来的节点」处确认。
之前所有讨论以这份为准。
零、最重要的背景：手机端实测体验很差
用户在手机上打开了 https://radar.orli.asia（WiFi 和流量都能打开，功能正常），但页面布局有严重问题：
问题
	
具体


顶部占 1.5 屏
	
标题、日期、数据源健康度、本期必看 5 条，全堆在首屏


卡片占满一屏
	
一屏只能看到 1 条卡片


按钮标签占一半
	
收藏/已读/👍👎/原链接/国内加速 一堆按钮，加上 3-4 个分类标签，占了卡片一半高度
这是当前最痛的问题，优先于任何其他功能。
一、移动端重构规范（本次核心任务）
设计目标
一屏至少看到 3-4 条卡片（现在是 1 条）。顶部区域压缩到不超过屏幕 15%。
参考对象：RSS 阅读器（Reeder、Inoreader、Feedly）的列表页，以及 Hacker News 移动版。
它们的共同点是：一行标题、一行摘要、一行元信息，紧凑排布，操作默认收起。
具体规范
顶部（目标 ≤ 80px，约屏幕 10%）
纯文本
每日 GitHub 雷达        2026-09-19    ⚙ ◐
∙
∙
标题、日期、设置、主题切换 压进一行
∙
∙
数据源健康度 → 收成一个小 ⓘ 图标，点击才展开
∙
∙
「本期必看」改成横向滚动卡片（不是纵向堆叠），高度 ≤ 120px
∙
∙
每条只显示：序号 + 仓库名 + 今日新增
∙
∙
可左右滑动看第 4、5 条
筛选区（目标 ≤ 2 行）
∙
∙
类别 Tab：改成横向滚动 chips（全部/AI Skill/爆发/学习成长/经典），一行可滑动
∙
∙
第二行：语言 ▾　排序 ▾　🔍　筛选 ▾
∙
∙
时间范围、只看未读、只看收藏、重置 → 全部收进「筛选」折叠面板，默认不展开
卡片列表（本次最关键的改动）
紧凑模式（默认）：
纯文本
cloudflare/security-audit-skill            ⭐
让 AI 编程助手给代码做安全体检，分几个阶段
查出漏洞，结果是一份可直接核对的清单
JavaScript · ★14.1k · +3,606 今日 · 2期
∙
∙
仓库名一行
∙
∙
中文一句话 最多 2 行，超出省略
∙
∙
元信息一行：语言 · 总星 · 今日新增（醒目色）· 持续期数
∙
∙
分类标签 最多显示 2 个，其余折叠成 +2
∙
∙
右侧只保留一个 ⭐ 收藏图标
展开模式（点击卡片后）才显示：
∙
∙
深度摘要（点击「展开摘要」）
∙
∙
📖 展开全文（如果有）
∙
∙
操作按钮行：收藏 / 已读 / 👍 / 👎 / 原链接 / 国内加速
∙
∙
备注输入框
间距与字号
∙
∙
卡片间距 8px（不是 24px）
∙
∙
卡片内边距 12px
∙
∙
正文 14-15px，仓库名 15-16px 加粗，元信息 12-13px 灰色
∙
∙
移除所有无意义的大块留白
硬性验收标准
用 Chrome DevTools 的 iPhone 模拟（或真机）检查：
∙
∙
[ ] 首屏（不滚动）能看到至少 3 条卡片
∙
∙
[ ] 顶部区域（标题+必看+筛选）不超过屏幕 15%
∙
∙
[ ] 滚动一屏能浏览 5 条以上
∙
∙
[ ] 收藏 ⭐ 不点开卡片就能操作
∙
∙
[ ] 桌面端（Mac）显示不变差——不要为了手机牺牲桌面
二、PWA + Web Push（本次做，成本为零）
PWA
添加 manifest.json + sw.js：
∙
∙
应用名「GitHub 雷达」，图标纯色 + 文字（不要复杂图形）
∙
∙
display: standalone（隐藏浏览器地址栏，看起来像原生 APP）
∙
∙
主题色跟暗色模式一致
∙
∙
Service Worker 缓存静态资源，离线可看上一期内容
Web Push
∙
∙
iOS 16.4+ 支持（必须先「添加到主屏幕」才能订阅）
∙
∙
页面加一个「开启推送」按钮，用户点后请求权限
∙
∙
每 3 天发布新一期时推送一条：「本期 20 条，5 条爆发」
∙
∙
⚠️ 推送需要 VAPID 密钥，请你生成并配进 Secrets，同时告诉我怎么配
如果 Web Push 在 iOS 上实测不稳定，如实告诉我，不要硬凑。
三、反馈闭环（必须做，这是核心缺口）
我检查了线上页面，没看到「调教雷达」入口，而且 👍👎 按钮目前似乎只是记录、不影响下期结果。
用户最初的核心需求之一就是"通过长期磨合不断完善"，如果这个没实现，等于核心功能缺失。
要求
1.
1.
👍 / 👎 / ⭐收藏 / 已读 的行为必须持久化（存 data/feedback.jsonl 并 commit 回仓库）
2.
2.
必须影响下期排序：
∙
∙
点赞过的条目的关键词、语言、作者 → 记入 boost
∙
∙
点踩过的 → 记入 mute，下期直接过滤同类
3.
3.
页面加「调教雷达」入口（输入框），用户可以自然语言输入：
∙
∙
「多给我看 Rust 和 AI Agent」
∙
∙
「别再推爬虫类」
∙
∙
保存进配置文件，下期生效
4.
4.
下期生成时必须读取​ feedback 并应用权重
四、配置 DeepSeek Key（配法有坑，注意）
报告里写的默认 LLM_MODEL=deepseek-chat 这个值已经不能用了——DeepSeek 官方 2026-07-24 退役了该模型名，请求会直接失败。
必须手动指定：
Secret
	
值


LLM_API_KEY
	
用户的 Key


LLM_BASE_URL
	
https://api.deepseek.com


LLM_MODEL
	
deepseek-flash（手动填，别用默认值）
请你把代码里的默认值也从 deepseek-chat 改成 deepseek-flash，避免以后有人踩坑。
配 Key 后请验证：手动触发一次 publish，看新条目是否自动生成了中文。
五、小问题修复
1.
1.
trending 来源的条目 Topics 显示「—」​ —— trending 抓取时没有 topics 字段。请从 Search API 补充，或抓取时一并获取
2.
2.
书架 + Obsidian 导出：给我傻瓜式验收步骤（打开哪里、点什么、怎么判断成功），用户没有开发基础
六、关于手机 APP（暂不做，先听我说明）
用户问能不能做 iOS 原生 APP。我的判断是技术可行但当前不划算，理由：
	
PWA（本次做）
	
原生 APP


成本
	
0
	
苹果开发者账号 ¥688/年，或免费账号 7 天重签一次


添加到主屏幕
	
用户自己点一下
	
需要 Xcode（几十 GB）+ 连线 + 信任证书


全屏无地址栏
	
✅
	
✅


离线
	
✅
	
✅


推送
	
Web Push（iOS 16.4+）
	
APNs（复杂得多，需要服务器推）


更新
	
自动
	
每次改功能要重新编译安装
用户没有开发基础，Xcode 的工程配置、证书、真机调试这些环节是 AI 帮不上忙的（要手动登录账号、点信任），这是最大的摩擦点。
所以：先把 PWA 做好，实测推送能不能成。如果 Web Push 在 iOS 上稳定，就没必要做原生 APP。
这一节你不用执行，是我给用户的说明。等用户确认后再说。
七、停下来的节点
只在以下情况找我：
1.
1.
需要我配 Secrets（给具体点击步骤：打开哪个网址、点哪里、填什么）
2.
2.
移动端重构做完了，要我验收（告诉我怎么在手机上测）
3.
3.
Web Push 在 iOS 上实测不稳定，需要决策
除此之外一路跑到底。
八、优先级
1.
1.
移动端重构（最痛，先做）
2.
2.
反馈闭环（核心缺口）
3.
3.
DeepSeek Key 配置 + 验证
4.
4.
PWA + Web Push
5.
5.
小问题修复
每做完一项停一下自检验收标准，不用等我确认。
您查看的是历史版本
如需编辑，可将画布还原到此版本。
返回最新版本
还原此版本